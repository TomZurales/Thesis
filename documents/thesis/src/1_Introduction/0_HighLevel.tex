\subsection{Problem Context}
During episodic operations, the visual features of an environment are expected to change over time. A map point may therefore be created for a visual feature which exists during operation $n$, but not operation $n + 1$. These map points are considered "outdated", and can lead to unbounded map growth without some method of identification and removal. Figure XXX shows a simplified representation of this scenario where on day 1 the map point $m$ is visible from some positions, but not visible from others due to an obstruction. On all other days, the visual feature represented by the map point gone (meaning the map point is outdated). The question of interest becomes ``When can the observer be confident that the map point can be discarded?'' Answering this question 

On day 2, the observer does not see the map point, but observation